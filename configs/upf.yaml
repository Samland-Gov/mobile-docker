logger:
  file:
    path: /var/log/open5gs/upf.log

global:

upf:
  pfcp:
    server:
      - address: upf.samcom
    client:
  gtpu:
    server:
      - address: upf.samcom
        advertise: docker-host.external-ip
  session:
    - subnet: 10.45.0.0/16
      gateway: 10.45.0.1
